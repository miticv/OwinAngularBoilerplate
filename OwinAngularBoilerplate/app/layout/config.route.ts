/// <reference path="../_all.ts" />


module app.layout {
    'use strict';

    /* we do not use routing here, since we always load layout controller once - regardless of the route */


    //angular.
    //    module('app.layout').
    //    config(LayoutConfig);
    //LayoutConfig.$inject = ['$stateProvider'];
    //function LayoutConfig($stateProvider: ng.ui.IStateProvider) {
    //    $stateProvider.
    //        state("start", {
    //            url: '/',
    //            controller: 'app.layout.LayoutController',
    //            controllerAs: 'layout',
    //            templateUrl: '/app/layout/layout.html',
    //        });
    //}

} 